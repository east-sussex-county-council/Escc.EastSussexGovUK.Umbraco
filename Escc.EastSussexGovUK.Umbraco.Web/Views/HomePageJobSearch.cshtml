@model Escc.EastSussexGovUK.Umbraco.Web.HomePage.HomePageViewModel
@if (Model.JobSearchResultsPage != null)
{
    <form action="@Model.JobSearchResultsPage.Url" method="get" class="job-search text-content">
        @if (Model.JobsLogo == null)
        {
            <div class="gutters">
                <h2>Search for jobs</h2>
            </div>
        }
        <div class="form short-form">
            @if (Model.JobsLogo != null)
            {
                if (Model.JobsHomePage != null)
                {
                    <h2><a href="@Model.JobsHomePage.Url"><img src="@Model.JobsLogo.ImageUrl" width="@Model.JobsLogo.Width" height="@Model.JobsLogo.Height" alt="Search for jobs" /><span class="aural">Search for jobs</span></a></h2>
                }
                else
                {
                    <h2><img src="@Model.JobsLogo.ImageUrl" width="@Model.JobsLogo.Width" height="@Model.JobsLogo.Height" alt="Search for jobs" /><span class="aural">Search for jobs</span></h2>
                }
            }
            <label for="keywords">Search for jobs</label>
            <input type="search" name="keywords" size="15" id="keywords" />
            <label for="locations">Location</label>
            <select name="locations" id="locations">
                <option value="" selected="selected">All</option>
                @foreach (var location in Model.JobLocations)
                {
                    <option value="@location.Text">@location.Text (@location.Count)</option>
                }
            </select>
            <label for="jobtypes">Category</label>
            <select name="jobtypes" id="jobtypes">
                <option value="" selected="selected">All</option>
                @foreach (var jobType in Model.JobTypes)
                {
                    <option value="@jobType.Text">@jobType.Text (@jobType.Count)</option>
                }
            </select>
            <input type="hidden" name="source" value="eastsussexgovuk-home" />
            <div>
                <input type="Submit" value="Search" />
            </div>
        </div>
    </form>
}